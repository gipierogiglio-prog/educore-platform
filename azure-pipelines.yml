# Azure Pipelines — DISABLED
# Pipeline desativada. Deploys feitos via SSH direto no servidor.
# Ver docs/deploy-pipeline.md para o fluxo atual.

trigger: none
pr: none

pool:
  vmImage: ubuntu-latest

steps:
  - script: echo "Pipeline desativada. Deploys via SSH direto."
    displayName: "⏸️ Pipeline desativada"
